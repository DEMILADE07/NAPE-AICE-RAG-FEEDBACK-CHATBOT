# Data Processing
pandas>=2.0,<3.0
# NumPy: Must be <2.0 for ChromaDB 0.4.22 and Streamlit compatibility
# ChromaDB uses deprecated np.float_ which was removed in NumPy 2.0
# Python 3.13 needs >=1.26.4, but <2.0 for compatibility
numpy>=1.26.4,<2.0

# Google Sheets API
gspread==6.1.2
google-auth==2.25.2
google-auth-oauthlib==1.2.0
google-auth-httplib2==0.2.0

# Database & Storage
chromadb==0.4.22
# Note: sqlite3 is built-in to Python, no installation needed

# Embeddings
sentence-transformers>=5.0.0
# PyTorch - allow newer versions for Python 3.13 compatibility
# Note: Python 3.13 requires torch>=2.4.0, older versions don't have wheels
torch>=2.0.0

# LLM
ollama==0.1.7  # For local development only
groq==0.4.2  # For Streamlit Cloud deployment (recommended)

# Web Interface
# Use latest Streamlit - newer versions may support NumPy 2.0 and Python 3.13
streamlit>=1.40.0
plotly==5.18.0

# Utilities
python-dotenv==1.0.0
# Pydantic: Upgrade to version compatible with Python 3.13
# pydantic-core 2.14.6 (from pydantic 2.5.3) doesn't support Python 3.13
pydantic>=2.9.0
Pillow>=10.0.0  # For image processing (header image)

